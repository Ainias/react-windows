export type WindowContainerDimension = {
    top: number;
    left: number;
    right: number;
    bottom: number;
};

export const WINDOW_CONTAINER_MIN_WIDTH = 150;
export const WINDOW_CONTAINER_MIN_HEIGHT = 50;
